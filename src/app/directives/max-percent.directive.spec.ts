import { MaxPercentDirective } from './max-percent.directive';

describe('MaxPercentDirective', () => {
  it('should create an instance', () => {
    const directive = new MaxPercentDirective();
    expect(directive).toBeTruthy();
  });
});
